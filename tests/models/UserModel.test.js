const UserModel = require("../../models/UserModel");
const expect = require("chai").expect;

describe("UserModel", () => {
    it("works", done => {
        expect(2).to.equal(2);
        done();
    })
});