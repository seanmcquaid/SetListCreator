describe("<ClientInfoPage/>", () => {

    beforeEach(() => {
        jest.useFakeTimers();
    });

    afterEach(() => {
        jest.useRealTimers();
    });

    test("Loading Spinner", () => {

    });

    test("Loads Client Info", () => {

    });

    test("Error on Client Info", () => {

    });

    test("Create Set List Button", () => {

    });

    test("In Progress when Set List Isn't Available", () => {

    });


});