import React from "react";

const ClientListPage = props => {
    return(
        <div>
            {/* Client - Status (in progress/submitted via client/complete) - Button to go to client's specific page*/}
        </div>
    )
};

export default ClientListPage;